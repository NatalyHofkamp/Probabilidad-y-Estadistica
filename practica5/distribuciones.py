import random
# 1. Utilizando únicamente la función random de su lenguaje (la función que genera un número aleato-
# rio uniforme entre 0 y 1), implemente una función que genere un número distribuido Bernoulli con
# probabilidad p.

def bernoulli ():
    return random.randrange(0,1,1);

# 2. Utilizando la función del punto anterior, implemente otra que genere un número binomial con los
# parámetros n, p.

def binomial(n,p):
    pass



# 3. Utilizando el procedimiento descrito en el capı́tulo 6 del Dekking (método de la función inversa o de
# Monte Carlo), implementar una función que permita generar un número aleatorio con distribución
# Exp(λ).






# 4. (Opcional) Investigar cómo generar números aleatorios con distribución normal. Implementarlo.